apiVersion: v1
kind: Service
metadata:
  name: etnair-api
  labels:
    app: etnair-api
spec:
  type: NodePort
  selector:
    app: etnair-api
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    nodePort: 30000
